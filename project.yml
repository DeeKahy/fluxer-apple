name: Fluxer
options:
  bundleIdPrefix: dev.deekahy
  deploymentTarget:
    iOS: "18.0"
    macOS: "15.0"
packages:
  FluxerKit:
    path: .
  LiveKit:
    url: https://github.com/livekit/client-sdk-swift
    from: 2.6.0
settings:
  base:
    SWIFT_VERSION: "6.0"
    GENERATE_INFOPLIST_FILE: YES
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: Y3MVULKQ9P
targets:
  Fluxer-iOS:
    type: application
    platform: iOS
    sources:
      - Apps/Shared
    dependencies:
      - package: FluxerKit
      - package: LiveKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.deekahy.fluxer
        PRODUCT_NAME: CornFlux
        INFOPLIST_KEY_NSMicrophoneUsageDescription: CornFlux uses the microphone for voice channels and calls.
        INFOPLIST_KEY_NSCameraUsageDescription: CornFlux uses the camera for video calls.
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        TARGETED_DEVICE_FAMILY: "1,2"
  Fluxer-macOS:
    type: application
    platform: macOS
    sources:
      - Apps/Shared
    dependencies:
      - package: FluxerKit
      - package: LiveKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: dev.deekahy.fluxer.mac
        CODE_SIGN_IDENTITY: "-"
        INFOPLIST_KEY_NSMicrophoneUsageDescription: CornFlux uses the microphone for voice channels and calls.
        INFOPLIST_KEY_NSCameraUsageDescription: CornFlux uses the camera for video calls.
        PRODUCT_NAME: CornFlux
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        ENABLE_HARDENED_RUNTIME: NO
